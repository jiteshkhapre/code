def prime(x,y):
    for i in range(x,y+1):
        count = 0
        for j in range(1,i+1):
            if i%j == 0:
                count += 1
        if count == 2:
            print(i)


a = int(input("Enter left interval: "))
b = int(input("Enter right interval: "))
res = prime(a,b)
